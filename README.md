# Private Japan Trip Page

This folder is ready to deploy to GitHub Pages.

- Upload only the files in this folder.
- Do not upload `../github-pages-trip-password.txt`.
- The trip HTML is encrypted inside `index.html`.
- The password is stored locally at `../github-pages-trip-password.txt`.

## Local Preview

Open `index.html` in Safari/Chrome and enter the password.
